# Findings log

## PR 1022 + 1029 scout (done)
- PR 1022 (review hub): evidenceService (agent-evidence fenced JSON self-reports), reviewWorkflowService + config/review-workflows.json (standard/hardened/full-gate chains, riskDefaults low->standard high->hardened critical->full-gate), visibility presets, multi-commander manager, custom-agent registry, shellSafety. Fully unique vs main. 187 commits stale, CONFLICTING.
- PR 1029 (JARVIS autopilot): supervisorService (tier-weighted urgency 1.5/1.15/0.6/0.35 + InterruptionBudget + DigestQueue), Codex app-server JSON-RPC client (structured signals vs PTY scraping), discordWatchService (cursor-based ambient channel ingest -> task records, regex extractor tables in config/discord-watch.json), realtime voice loop, jarvis-panel (Alt+J). Atlas files on this branch are SUPERSEDED by main (main strictly larger + 9 extra files). 185 commits stale, CONFLICTING.
- Both touch server/index.js, client/app.js, client/index.html, client/styles.css, CODEBASE_DOCUMENTATION.md -> mutual conflicts.
- Keep-regardless concepts: evidence-driven review, chains-as-data + p_chain math, tier-weighted interruption budget, structured control-plane over PTY scraping, cursor-based external-feed ingest.

## PR 1043 + 1041 scout (done)
- PR 1043 implements the full voice tier ladder: T0 faster-whisper -> T1 exact-phrase (<50ms) -> T2 Bonsai-1.7B via llama.cpp :5742, grammar-constrained JSON + deterministic guard layer (registry-grounded entity resolution, homophone fixes, question-shape override) -> T2.5 voiceQueryService (deterministic gh/activity answers, no LLM) -> T3 local brain (Qwen3.5-9B) -> T4 Commander (sonnet-low default). confidence>=0.6 gate. Transcripts logged to ~/.orchestrator/voice-transcripts.jsonl for GEPA recalibration (prompt v14 hit 66/66 eval).
- voiceRegistryService: config/voice-registry.json + ~/.orchestrator overlay, longest-alias-first, people/projects/products. "Aliases are load-bearing."
- Confirm-first destructive regex + 30s pendingConfirmation window in voiceBrainService (854 lines).
- realtimeManagerService: transition-only narration, 30s floor, silent in background mode.
- reviewChainService (182 lines) + config/review-chains.json: default (codex->sonnet), high-risk (codex->opus), quick (sonnet). Read-only reviewer spawns (codex exec -s read-only / claude -p with allowlisted tools). VERDICT: approved | needs_fix parsing; needs_fix -> task record + forward to implementer session via Commander; NO auto re-run after fix (doc says re-run, code stops).
- PR 1043 also carries duplicate copies of supervisor/discord-watch/app-server (same as 1029) AND a stale atlas fork. 185 behind, CONFLICTING, atlas subtree must be dropped on rebase.
- PR 1041 = PLANS/2026-08-15/VOICE_TIERED_ARCHITECTURE.md spec, MERGEABLE as-is, safe to land any time.
- Doc verified: "Hermes agents: not a thing in this codebase (zero references)."
- NOTE: 1043 and 1029 overlap heavily (supervisor, discordWatch, appServer appear in BOTH). Plan must pick one canonical landing path.

## GitHub Projects v2 research (done)
- Fields: text/number/date/single-select/iteration; 50 fields/project, 50 options/select. Views: table/board/roadmap, NO calendar. Advanced AND/OR search GA 2026-07.
- Item limit 50k (GA 2025-04), archive 10k. Sub-issues (100/parent, 8 deep) + issue types (25/org) GA 2025-04; issue dependencies blocked-by/blocking GA 2025-08 (50 per relationship); gh CLI manages all three since 2026-06. Tasklist blocks dead (2025-04).
- Automation: built-in workflows (item added/reopened/closed, PR merged -> status, auto-add by query [Free: 1, Team: 5], auto-archive). NO native due-date reminders anywhere (community-requested, unshipped); milestones due_on silent; Slack scheduled reminders = PR reviews only. Teams use Actions cron + GraphQL.
- API: GraphQL only (no REST), projects_v2_item webhook includes prev+current values (2024-06); gh project subcommands cover fields/items; rate limits trivial for 5-min polling. Fine-grained PAT support UNVERIFIED.
- Images via bots: no official API; undocumented uploads.github.com/user-attachments endpoint works (unofficial, could die); safest = commit to assets repo. Discord CDN URLs expire, never hotlink.
- Access: Free org + outside collaborators = testers free; on Team plan every private-repo viewer needs a $4 seat. Mobile Projects = view/move/comment only, adding items broken (as of last reports).
- Insights charts: Free ~2 saved charts private, Team+ unlimited (best-effort).

## Final review round: Fable reviewer (done, fix-then-ship)
1. Ambient discordWatch -> task records only = no card/date/reminder; 30-day incident survives. Rewire to dated card proposals via triage queue; drop "structurally impossible".
2. Reminder loop needs dead-man switch: daily even-when-empty digest, prod-instance-only, failure alerting as spec requirement (auto-trello silent-401 precedent).
3. Auto-launch trust: 1% lane + batch-launch feed yolo agents untrusted card text w/ credential access; require human-applied auto-eligible marker (UI-only), stamp card origin, name the exposure; auto-merge config under supervisor deny list.
4. Split phase 0 into 0a (quick: 1041, 1083/1081/1085, one-liners) / 0b (extraction train, gates only phases 3-4); fix THE_PLAN_SIMPLE build order to match Sequencing.
5. Trello HQ status cards: agent-generated, not hand-maintained.
6. Review-chain reconciliation: consider inverse (keep 1043 engine w/ security boundary, port 1022 config/risk defaults; evidence as follow-up). Re-decide by diff size.
7. Pin-reaction collision: bot "captured" pin == human "acknowledged" pin; use distinct emojis. Mark ack clocks/WIP as advisor-report-only.
8. Verify Trello tier: free workspace caps at 10 open boards; consolidation needs Standard+. State tier.
9. Add 3 adoption metrics to Friday close-out (committed-without-date, overdue trend, capture vs ambient-caught).
10. Ambient watcher needs mute/correct affordance; phone capture = Discord-only until ADHD deploys (say so); weekly "what shipped" digest.

## Final review round: Opus reviewer (done, fix-then-ship, 22 findings)
SECURITY (1-5): parseVerdict matches FIRST "VERDICT: approved" anywhere in output -> PR content self-approves; fix: last-line anchor + nonce + multi-match=inconclusive. Reviewer summary text injected unsanitized into Commander forwarder (skipPermissions) -> needs quoting contract or human inbox. agentSpawnHelper (in NO train step) spawns with skipPermissions/yolo FALLBACK_SPAWN_FLAGS; need two-mode spawn contract (reviewer=detached read-only, fixer=permissioned). batchLaunch writes card text verbatim to PTY (only DA-report stripped): bare \r submits early, ESC passes; need CR/ESC/C0 sanitizer + no auto-launch for open-channel cards. /api/commander/send-to-session has NO route auth (index.js:8204); gate like requireDiscordAccess in same PR as removing bot fallback.
STATE (6-11): user-settings.json is per-checkout + gitignored (master has the 2 mappings, dev has 0) -> "phase 1 config" lands in one checkout only; move global.ui.tasks.* to data dir or studio-state repo. NO Discord webhook sender exists anywhere in server/; notificationService requires sessionId; need webhook client + card-scoped notification + escalation state machine as data. listBoardCards omits dueComplete + customFieldItems -> reminder loop and Priority unreadable from snapshot; add fields + write snapshot schema. No 429/backoff anywhere (httpJson); need shared token bucket (Trello 300/10s key, 100/10s token) across UI/batch/PR-poll/triage/auto-trello. Two instances share data dir: TaskRecordService read-once + whole-file overwrite, no tmp-rename, no lock; automations need single-instance lease. Data-dir flip hazard: both dirs have exactly 11 workspace files; adding one workspace can flip stores; config.json already points workspaces at legacy dir while records use new; need one-time merge + pin AGENT_WORKSPACE_DIR; also migrate ~/.orchestrator/voice-registry.json (5678 bytes live).
TRAIN (12-14): fix-PR overlap matrix wrong; #1081 is 35-file e2e harness refactor -> land #1081 FIRST or assign conversion; #1043 and #1029 share 11 MORE files (all voice core; voiceBrainService 854 vs 303 lines) -> need file-to-branch source-of-truth table (#1043 wins voice, #1029 wins supervisor/watch/appServer); voiceCommandService on 4 branches needs own merged-state statement. shellSafety consumers are in step 10 not 6 (move together); prReviewAutomationService belongs in step 6 (+154 lines, needs agentSpawnHelper, reads evidence); #1022 plugin subsystem in NO step (add or declare dropped).
SPECS (15-21): supervisor urgency has NO priority term; P0*T3*warn=48 < threshold 60, P3*T1*critical=84 > threshold; write composed formula + P0 floor max(score, alwaysInterruptAbove). Three docs give 3 incompatible alert policies; name ONE interruption-policy owner + config. Triage agent: no trigger/schema/idempotency/budget/failure spec; Atlas proposals = pattern to copy not store to reuse; repo-map dependency = empty Atlas registry, explicit prerequisite. Fleet: 5 endpoints need request/response schemas + token lifecycle + idempotency key derivation. Limits publisher: cadence, staleness age, available:false != drained, privacy note. Context compiler: manifest/profile schemas, drift check, 682-file migration story. Testability: reminder loop (injectable clock + fixtures), triage eval set, compiler golden test, fleet contract test, attachment re-host time test.
FACTS (22): teammates list NOT empty (1 entry: Anrokx, hyfire2/epic-survivors). promptArtifactService EXISTS on main (233 lines, routes 6647-6708, encrypted repo-backed prompts; ~/.agent-workspace/prompts/ empty) -> move to built-never-exercised; phase 5 item 3 cheaper. Only ONE hardcoded getProvider('trello') (taskDependencyService:156). "2 of 11" true of master checkout only. Possible 12th board: Epic Survivors QA Automation in TRELLO_BOARDS.md.

## Final review round: Codex Sol-high (done, verdict REWORK, 14 findings)
1. Card text -> skipPermissions prompt: untrusted quoted data w/ source id+hash; external cards need human approval OR read-only planner returning strict schema to trusted broker; triage gets narrow card ops only; prompt-injection tests.
2. Fleet auth: one bearer AUTH_TOKEN (also accepted in query params, index.js:316) + caller-supplied member header = leaked token -> RCE (agentManager.js:201 concatenates unvalidated model/reasoning/resume into shell). Need per-peer hashed revocable credentials w/ capabilities (read-limits, request-launch), no query tokens, device-bound identity, server-side allowlists, local acceptance, never proxy terminal-input.
3. discordWatch (1029): creates local discord:* items (violates Trello-only-store); "on it"/"done" binds to LATEST open item in channel (wrong-work); truncates >500; cursor advances w/o card; ticketProvider 'discord' silently rejected by allowlist; maps priority onto tiers. Fix: durable messageID->cardID inbox, ack only after card-or-ignored, claims must reference reply/cardID/nonce, archive not truncate, separate priority vs tier fields.
4. Review chains: #1022 reviewers RUN gh pr comment/review (writes) so "read-only reviewer" + GitHub-polling detection are incompatible as combined; fix: reviewer returns strict JSON to parent, trusted parent validates (exit, schema, identity, stage, PR head SHA) and posts the review; invalidate passes on head change; required GitHub check incl. human gate.
5. Dual instances: no separate data dirs in docs; batchLaunch allocates worktree numbers from ONE workspace (bypasses cross-workspace allocator index.js:2933); need dev data dir + automation leader election + lock/CAS atomic state + repo-wide allocator checking git worktree list + durable idempotency keys (card+op+revision) + launch-phase records w/ compensation.
6. Reminder loop: provider has NO create/add/remove label ops (updateCard idLabels replaces whole set) and NO attachment WRITE (read-only, trelloProvider:268); alert state must be per-occurrence {cardId,dueRevision,kind,channel,scheduledAt,status,attempts}; one escalation table w/ timezone/quiet hours/budget/retry/claim semantics; claim = assignment/comment/distinct reaction; recurrence spec for TZ/DST/missed runs.
7. v1 cross-machine handoff broken as specced: batchLaunch reads only title+desc (not artifacts/comments/attachments); no Machine field in phase 1; two machines can double-launch. Fix: versioned prompt-artifact schema in deterministic Trello location (author, source hash, expiry, content hash), preview+accept before launch, card-scoped launch lease w/ nonce+expiry rechecked before spawn, record machine+session on card.
8. workspace access field: workspaceManager:844 OVERWRITES access with GitHub repo visibility; "finish the wiring" unsafe; need migration separating workspaceAudience/repositoryVisibility/ACLs, subject from authenticated peer creds, deny-unknown, authz tests.
9. Limits wire shape WRONG in plan: Codex+Grok return `windows` arrays not fiveHour/sevenDay (usageLimitsService:257,354). Admission hook = allow/block only, post-agent-selection; need versioned provider-neutral window array {provider,bucket,duration,usedPct,reset,observedAt,stale,machine,member} + separate routing phase returning immutable launch plan + composite admission list + local guard recheck before spawn.
10. Provider-neutral reminders/triage impossible without normalization FIRST: make normalized ticket model + capability contract a phase 1 prerequisite (status, due state, priority, assignee, labels, attachments, comments, revision); widen provider ids; task-record priority field + queue sort are currently absent and unscheduled.
11. Attachment pipeline spec: multipart upload op, download Discord bytes BEFORE ack, allowlist hosts, reject private-address redirects, byte/MIME limits, content sniff, persist message/card/source/target ids for retry, capture-failed state (no success pin on failure).
12. Context compiler: ownership manifest, refuse unmanaged overwrites, lock + tmp + validate + atomic replace, last-known-good, dry-run diff, missing-required-fragment = error, roles select instructions NEVER grant permissions.
13. GH Projects: Codex claims REST endpoints now exist (projects/items/fields/views) + fine-grained PATs + projects_v2_item webhook is public-preview. CONFLICTS with our web scout ("GraphQL only, confirmed"). VERIFY INDEPENDENTLY before editing swap doc.
14. Replace phase numbers with explicit dependency DAG; voice needs end-to-end acceptance matrix (each input surface through create/update/confirm/launch/result/retry/duplicate/restart/visible-failure); don't claim voice complete from lane tests.
