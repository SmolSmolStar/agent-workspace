# User report
1. Fresh/Continue/Resume overlay keeps appearing on F5 over box2d-luau work2 even though
   Claude is running there.
2. Duplicate/overlapping streamed text in Commander (user partially retracted this in the
   Commander session itself; pane capture shows clean content, so it is transient
   browser-canvas staleness healed by the existing 15s sweep).
